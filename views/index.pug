extends layout.pug 
//- Pulls content from layout.pug
block content
    section#content
        h2 Welcome, #{name}!
        form(action ='/goodbye', method='post')
            button(type='submit') Logout
            //- creates logout button  